name: Preview Deploy
# Architecture: Fargate run-task + Cloudflare DNS A record
#
# Each preview environment is a Fargate task running in a public subnet with a
# public IP. There is no ALB or load balancer — traffic flows:
#
#   Visitor (HTTP) → DNS A record → Fargate task public IP (port 80)
#
# An unproxied Cloudflare DNS A record is created per branch pointing directly
# to the container's public IP (TTL 60). The record is written here after the
# container passes a health check, and removed by preview-teardown.yml.
#
# Container startup: the generic `reckless/php:{version}` ECR image clones the
# repo at startup using an SSH deploy key (base64-encoded in DEPLOY_KEY env var),
# runs composer/npm, migrates, seeds, and serves via nginx + PHP-FPM.

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # OIDC
      pull-requests: write  # PR comments

    steps:
      # ── Context ──────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read project config
        id: cfg
        run: |
          echo "php=$(jq -r .php .claude/project.json)" >> $GITHUB_OUTPUT
          echo "stack=$(jq -r .stack .claude/project.json)" >> $GITHUB_OUTPUT
          echo "build=$(jq -r '.preview.build // "generic"' .claude/project.json)" >> $GITHUB_OUTPUT
          echo "region=$(jq -r .preview.region .claude/project.json)" >> $GITHUB_OUTPUT
          echo "project_slug=$(jq -r .preview.slug .claude/project.json)" >> $GITHUB_OUTPUT
          echo "domain=$(jq -r .preview.domain .claude/project.json)" >> $GITHUB_OUTPUT
          echo "aws_role_arn=$(jq -r .preview.aws.role_arn .claude/project.json)" >> $GITHUB_OUTPUT
          echo "execution_role_arn=$(jq -r .preview.aws.execution_role_arn .claude/project.json)" >> $GITHUB_OUTPUT
          echo "assets=$(jq -r '.preview.assets // ""' .claude/project.json)" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.cfg.outputs.aws_role_arn }}
          aws-region: ${{ steps.cfg.outputs.region }}

      - name: Derive names
        id: slug
        run: |
          REF="${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
          BRANCH_SLUG=$(echo "$REF" \
            | tr '[:upper:]' '[:lower:]' \
            | sed 's/[^a-z0-9-]/-/g' \
            | sed 's/--*/-/g' \
            | sed 's/^-//' \
            | sed 's/-$//' \
            | cut -c1-50)

          PROJECT_SLUG="${{ steps.cfg.outputs.project_slug }}"
          PREVIEW_KEY="${BRANCH_SLUG}--${PROJECT_SLUG}"

          echo "branch_slug=$BRANCH_SLUG" >> $GITHUB_OUTPUT
          echo "preview_key=$PREVIEW_KEY" >> $GITHUB_OUTPUT
          echo "task_family=preview-${BRANCH_SLUG}" >> $GITHUB_OUTPUT

      # ── Build image (dockerfile projects only) ───────────────
      - name: Log in to ECR
        if: steps.cfg.outputs.build == 'dockerfile'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Derive image name
        if: steps.cfg.outputs.build == 'dockerfile'
        id: image
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          REGISTRY="$ACCOUNT_ID.dkr.ecr.${{ steps.cfg.outputs.region }}.amazonaws.com"
          REPO="preview/${{ github.event.repository.name }}"
          aws ecr describe-repositories --repository-names "$REPO" 2>/dev/null \
            || aws ecr create-repository --repository-name "$REPO" --image-scanning-configuration scanOnPush=false
          IMAGE="$REGISTRY/$REPO:${{ steps.slug.outputs.branch_slug }}-${GITHUB_SHA::8}"
          echo "uri=$IMAGE" >> $GITHUB_OUTPUT
          if aws ecr describe-images --repository-name "$REPO" \
            --image-ids imageTag="${{ steps.slug.outputs.branch_slug }}-${GITHUB_SHA::8}" \
            2>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Set up Docker Buildx
        if: steps.cfg.outputs.build == 'dockerfile' && steps.image.outputs.exists == 'false'
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        if: steps.cfg.outputs.build == 'dockerfile' && steps.image.outputs.exists == 'false'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.image.outputs.uri }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            PHP_VERSION=${{ steps.cfg.outputs.php }}

      # ── Resolve image URI ─────────────────────────────────────
      - name: Resolve image URI
        id: resolved
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          REGISTRY="$ACCOUNT_ID.dkr.ecr.${{ steps.cfg.outputs.region }}.amazonaws.com"
          if [ "${{ steps.cfg.outputs.build }}" = "dockerfile" ]; then
            echo "image=${{ steps.image.outputs.uri }}" >> $GITHUB_OUTPUT
          else
            echo "image=${REGISTRY}/reckless/php:${{ steps.cfg.outputs.php }}" >> $GITHUB_OUTPUT
          fi

      # ── Check for running task (git pull fast path) ───────────
      - name: Check for running task
        id: task
        run: |
          TASK_ARN=$(aws ecs list-tasks \
            --cluster default \
            --family "${{ steps.slug.outputs.task_family }}" \
            --desired-status RUNNING \
            --query 'taskArns[0]' --output text 2>/dev/null || echo "None")

          if [ "$TASK_ARN" != "None" ] && [ -n "$TASK_ARN" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "arn=$TASK_ARN" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      # ── Fast path: git pull in running container ──────────────
      # Pulls latest code and runs the project's deploy script if provided,
      # otherwise falls back to standard Laravel artisan commands.
      # The cold-start post-provision hook is NOT replayed — it's one-time.
      - name: Update running container (git pull)
        if: steps.task.outputs.exists == 'true'
        run: |
          aws ecs execute-command \
            --cluster default \
            --task "${{ steps.task.outputs.arn }}" \
            --container app \
            --command "/bin/sh -c 'git config --global --add safe.directory /var/www/html && cd /var/www/html && git pull && { [ -f .preview-env ] && . ./.preview-env; }; if [ -f docker/preview-deploy.sh ]; then . docker/preview-deploy.sh; elif [ -f artisan ]; then php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force; fi'" \
            --interactive

      # ── Fast path: ensure DNS exists ─────────────────────────
      # If the first deploy failed its health check, DNS was never created.
      # Check on every fast-path deploy and create it if missing — self-healing.
      - name: Ensure DNS record (fast path)
        if: steps.task.outputs.exists == 'true'
        env:
          CF_API_TOKEN: ${{ secrets.PREVIEWS_CLOUDFLARE_API_TOKEN }}
          CF_ZONE_ID: ebad49274edc8f01b668fffbf8ae15d5
        run: |
          HOSTNAME="${{ steps.slug.outputs.preview_key }}.${{ steps.cfg.outputs.domain }}"

          # Check if DNS record already exists
          EXISTING=$(curl -s \
            "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/dns_records?type=A&name=${HOSTNAME}" \
            -H "Authorization: Bearer $CF_API_TOKEN" \
            | jq -r '.result[0].id // empty')

          if [ -n "$EXISTING" ]; then
            echo "DNS record already exists for $HOSTNAME — nothing to do"
            exit 0
          fi

          # Resolve the running task's public IP
          ENI=$(aws ecs describe-tasks \
            --cluster default --tasks "${{ steps.task.outputs.arn }}" \
            --query "tasks[0].attachments[0].details[?name=='networkInterfaceId'].value" \
            --output text 2>/dev/null)

          PUBLIC_IP=$(aws ec2 describe-network-interfaces \
            --network-interface-ids "$ENI" \
            --query 'NetworkInterfaces[0].Association.PublicIp' \
            --output text 2>/dev/null)

          if [ -z "$PUBLIC_IP" ] || [ "$PUBLIC_IP" = "None" ]; then
            echo "Could not resolve public IP for task — skipping DNS"
            exit 0
          fi

          echo "Creating missing DNS A record: $HOSTNAME → $PUBLIC_IP"
          curl -s -X POST \
            "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/dns_records" \
            -H "Authorization: Bearer $CF_API_TOKEN" \
            -H "Content-Type: application/json" \
            --data "{\"type\":\"A\",\"name\":\"${HOSTNAME}\",\"content\":\"${PUBLIC_IP}\",\"ttl\":1,\"proxied\":true}" \
            | jq -r 'if .success then "DNS record created: \(.result.name) → \(.result.content)" else "ERROR: \(.errors)" end'

      # ── Build assets (s3-cached mode) ────────────────────────
      # Projects opt in via "assets": "s3-cached" in project.json.
      # The project must provide docker/preview-assets.sh which builds assets
      # and writes them to /tmp/assets.tar.gz. The hash key is computed from
      # that script's content + any files it lists on the first line as:
      #   # hash-inputs: file1 file2 ...
      # If no hash-inputs comment is present, the script itself is the key.
      - name: Build and cache assets
        if: steps.cfg.outputs.assets == 's3-cached'
        id: assets
        run: |
          SCRIPT="docker/preview-assets.sh"
          if [ ! -f "$SCRIPT" ]; then
            echo "ERROR: assets=s3-cached but $SCRIPT not found"
            exit 1
          fi

          # Compute hash: script content + files/dirs listed in "# hash-inputs:" comment
          HASH_INPUTS=$(grep -m1 '^# hash-inputs:' "$SCRIPT" | sed 's/^# hash-inputs://' || true)
          ASSET_HASH=$( { cat "$SCRIPT"; find ${HASH_INPUTS} -type f 2>/dev/null | sort | xargs cat 2>/dev/null; } | sha256sum | cut -c1-16)
          S3_KEY="assets/${{ github.event.repository.name }}/${ASSET_HASH}.tar.gz"
          BUCKET="r3-preview-assets"

          if aws s3api head-object --bucket "$BUCKET" --key "$S3_KEY" 2>/dev/null; then
            echo "Cache hit: $S3_KEY"
          else
            echo "Cache miss — building assets..."
            bash "$SCRIPT"
            if [ ! -f /tmp/assets.tar.gz ]; then
              echo "ERROR: $SCRIPT did not produce /tmp/assets.tar.gz"
              exit 1
            fi
            aws s3 cp /tmp/assets.tar.gz "s3://${BUCKET}/${S3_KEY}"
            echo "Uploaded to s3://${BUCKET}/${S3_KEY}"
          fi

          PRESIGNED_URL=$(aws s3 presign "s3://${BUCKET}/${S3_KEY}" --expires-in 3600)
          echo "url=$PRESIGNED_URL" >> $GITHUB_OUTPUT

      # ── Slow path: launch new task ────────────────────────────
      - name: Register task definition
        if: steps.task.outputs.exists == 'false'
        id: taskdef
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)

          # Build environment array — infrastructure/dynamic vars always set
          PREVIEW_HOST="${{ steps.slug.outputs.preview_key }}.${{ steps.cfg.outputs.domain }}"
          ENV_VARS="[
            {\"name\": \"APP_KEY\", \"value\": \"${{ secrets.PREVIEWS_APP_KEY }}\"},
            {\"name\": \"APP_URL\", \"value\": \"https://${PREVIEW_HOST}\"},
            {\"name\": \"SESSION_DOMAIN\", \"value\": \"${PREVIEW_HOST}\"},
            {\"name\": \"REPO_ORG\", \"value\": \"${{ github.repository_owner }}\"},
            {\"name\": \"REPO_NAME\", \"value\": \"${{ github.event.repository.name }}\"},
            {\"name\": \"BRANCH\", \"value\": \"${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}\"},
            {\"name\": \"DEPLOY_KEY\", \"value\": \"$(base64 -w0 docker/preview-deploy-key 2>/dev/null || true)\"}
          ]"

          # Merge project-specific env vars from docker/preview.env if present
          if [ -f docker/preview.env ]; then
            while IFS='=' read -r key value || [ -n "$key" ]; do
              [ -z "$key" ] || [ "${key#\#}" != "$key" ] && continue
              ENV_VARS=$(echo "$ENV_VARS" | jq --arg k "$key" --arg v "$value" '. + [{"name": $k, "value": $v}]')
            done < docker/preview.env
          fi

          ASSETS_URL="${{ steps.assets.outputs.url }}"
          if [ -n "$ASSETS_URL" ]; then
            ENV_VARS=$(echo "$ENV_VARS" | jq --arg url "$ASSETS_URL" '. + [{"name": "ASSETS_URL", "value": $url}]')
          fi

          TASK_DEF_ARN=$(aws ecs register-task-definition \
            --family "${{ steps.slug.outputs.task_family }}" \
            --network-mode awsvpc \
            --requires-compatibilities FARGATE \
            --runtime-platform "operatingSystemFamily=LINUX,cpuArchitecture=X86_64" \
            --cpu 1024 \
            --memory 2048 \
            --execution-role-arn "arn:aws:iam::${ACCOUNT_ID}:role/ecs-preview-execution" \
            --task-role-arn "arn:aws:iam::${ACCOUNT_ID}:role/ecs-preview-task" \
            --tags key=purpose,value=preview key=preview-slug,value="${{ steps.slug.outputs.preview_key }}" \
            --container-definitions "[{
              \"name\": \"app\",
              \"image\": \"${{ steps.resolved.outputs.image }}\",
              \"portMappings\": [{\"containerPort\": 80, \"protocol\": \"tcp\"}],
              \"environment\": $(echo "$ENV_VARS"),
              \"logConfiguration\": {
                \"logDriver\": \"awslogs\",
                \"options\": {
                  \"awslogs-group\": \"/ecs/preview\",
                  \"awslogs-region\": \"${{ steps.cfg.outputs.region }}\",
                  \"awslogs-stream-prefix\": \"ecs\",
                  \"awslogs-create-group\": \"true\"
                }
              }
            }]" \
            --query 'taskDefinition.taskDefinitionArn' --output text)
          echo "arn=$TASK_DEF_ARN" >> $GITHUB_OUTPUT

      - name: Run task
        if: steps.task.outputs.exists == 'false'
        id: run
        run: |
          TASK_ARN=$(aws ecs run-task \
            --cluster default \
            --launch-type FARGATE \
            --task-definition "${{ steps.taskdef.outputs.arn }}" \
            --tags key=purpose,value=preview key=preview-slug,value="${{ steps.slug.outputs.preview_key }}" \
            --network-configuration "awsvpcConfiguration={
              subnets=[subnet-60a76a09,subnet-2c73a757,subnet-62ac422f],
              securityGroups=[sg-0d773f640262948ac],
              assignPublicIp=ENABLED
            }" \
            --enable-execute-command \
            --query 'tasks[0].taskArn' --output text)
          echo "arn=$TASK_ARN" >> $GITHUB_OUTPUT

      # ── Get public IP ─────────────────────────────────────────
      - name: Wait for public IP
        if: steps.task.outputs.exists == 'false'
        id: ip
        run: |
          TASK_ARN="${{ steps.run.outputs.arn }}"
          echo "Waiting for task to get a public IP..."
          for i in $(seq 1 30); do
            ENI=$(aws ecs describe-tasks \
              --cluster default --tasks "$TASK_ARN" \
              --query "tasks[0].attachments[0].details[?name=='networkInterfaceId'].value" \
              --output text 2>/dev/null)

            if [ -n "$ENI" ] && [ "$ENI" != "None" ]; then
              PUBLIC_IP=$(aws ec2 describe-network-interfaces \
                --network-interface-ids "$ENI" \
                --query 'NetworkInterfaces[0].Association.PublicIp' \
                --output text 2>/dev/null)

              if [ -n "$PUBLIC_IP" ] && [ "$PUBLIC_IP" != "None" ]; then
                echo "Public IP: $PUBLIC_IP"
                echo "public_ip=$PUBLIC_IP" >> $GITHUB_OUTPUT
                break
              fi
            fi

            echo "  Waiting... (attempt $i/30)"
            sleep 10
          done

          if [ -z "$(grep public_ip $GITHUB_OUTPUT 2>/dev/null | cut -d= -f2)" ]; then
            echo "Timed out waiting for public IP"
            exit 1
          fi

      # ── Wait for app to respond ───────────────────────────────
      - name: Wait for app to respond
        if: steps.task.outputs.exists == 'false'
        run: |
          IP="${{ steps.ip.outputs.public_ip }}"
          TASK_ID=$(basename "${{ steps.run.outputs.arn }}")
          LOG_STREAM="ecs/app/${TASK_ID}"
          NEXT_TOKEN=""
          echo "Waiting for app to respond at http://$IP/ ..."
          for i in $(seq 1 60); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 "http://$IP/" 2>/dev/null || true)
            if [ -n "$STATUS" ] && [ "$STATUS" != "000" ] && [ "$STATUS" -lt 500 ]; then
              echo "App is responding (HTTP $STATUS)"
              break
            fi
            if [ $i -eq 60 ]; then
              echo "Timed out waiting for app to respond"
              exit 1
            fi
            echo "  Not ready yet (attempt $i/60, status: ${STATUS:-none})"
            # Print new container log lines since last check
            if [ -z "$NEXT_TOKEN" ]; then
              LOGS=$(aws logs get-log-events \
                --log-group-name /ecs/preview \
                --log-stream-name "$LOG_STREAM" \
                --region "${{ steps.cfg.outputs.region }}" \
                --limit 20 \
                --output json 2>/dev/null || true)
            else
              LOGS=$(aws logs get-log-events \
                --log-group-name /ecs/preview \
                --log-stream-name "$LOG_STREAM" \
                --region "${{ steps.cfg.outputs.region }}" \
                --next-token "$NEXT_TOKEN" \
                --limit 20 \
                --output json 2>/dev/null || true)
            fi
            if [ -n "$LOGS" ]; then
              echo "$LOGS" | jq -r '.events[].message' 2>/dev/null | sed 's/^/  [container] /' || true
              NEW_TOKEN=$(echo "$LOGS" | jq -r '.nextForwardToken' 2>/dev/null || true)
              [ -n "$NEW_TOKEN" ] && NEXT_TOKEN="$NEW_TOKEN"
            fi
            sleep 10
          done

      # ── Create Cloudflare DNS A record ───────────────────────
      - name: Create DNS A record
        if: steps.task.outputs.exists == 'false'
        env:
          CF_API_TOKEN: ${{ secrets.PREVIEWS_CLOUDFLARE_API_TOKEN }}
          CF_ZONE_ID: ebad49274edc8f01b668fffbf8ae15d5
        run: |
          HOSTNAME="${{ steps.slug.outputs.preview_key }}.${{ steps.cfg.outputs.domain }}"
          PUBLIC_IP="${{ steps.ip.outputs.public_ip }}"

          # Delete any existing record with this name (idempotent re-deploy)
          EXISTING_ID=$(curl -s \
            "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/dns_records?type=A&name=${HOSTNAME}" \
            -H "Authorization: Bearer $CF_API_TOKEN" \
            | jq -r '.result[0].id // empty')

          if [ -n "$EXISTING_ID" ]; then
            echo "Deleting existing DNS record $EXISTING_ID for $HOSTNAME"
            curl -s -X DELETE \
              "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/dns_records/${EXISTING_ID}" \
              -H "Authorization: Bearer $CF_API_TOKEN" > /dev/null
          fi

          echo "Creating DNS A record: $HOSTNAME → $PUBLIC_IP"
          curl -s -X POST \
            "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/dns_records" \
            -H "Authorization: Bearer $CF_API_TOKEN" \
            -H "Content-Type: application/json" \
            --data "{\"type\":\"A\",\"name\":\"${HOSTNAME}\",\"content\":\"${PUBLIC_IP}\",\"ttl\":1,\"proxied\":true}" \
            | jq -r 'if .success then "DNS record created: \(.result.name) → \(.result.content)" else "ERROR: \(.errors)" end'

      # ── Post PR comment ───────────────────────────────────────
      - name: Post preview URL
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const slug = '${{ steps.slug.outputs.preview_key }}';
            const domain = '${{ steps.cfg.outputs.domain }}';
            const url = `https://${slug}.${domain}`;
            const body = [
              '## Preview Environment',
              '',
              `**[${url}](${url})**`,
              '',
              `Branch: \`${process.env.GITHUB_HEAD_REF || process.env.GITHUB_REF_NAME}\``,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c => c.body?.includes('## Preview Environment'));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

            core.summary.addRaw(body).write();
